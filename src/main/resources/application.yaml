spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/board
    username: admin
    password: admin1234
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  